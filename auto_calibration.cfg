#####################################################################
#                    SISTEMA DE CALIBRAÇÃO AUTOMÁTICA              #
#                     Tool Offsets Auto-Calibration                #
#####################################################################

#####################################################################
#                    CALIBRAÇÃO COMPLETA AUTOMÁTICA               #
#####################################################################

[gcode_macro CAL_FULL]
description: Sequência completa de calibração automática da impressora
gcode:
    RESPOND MSG="🚀 ===== INICIANDO CALIBRAÇÃO COMPLETA ====="
    RESPOND MSG="⏱️ Tempo estimado: 15-20 minutos"
    
    # 1. Verificações iniciais de segurança
    RESPOND MSG="🔍 1/7 - Verificações de segurança..."
    {% if printer.toolhead.homed_axes != "xyz" %}
        RESPOND MSG="📍 Fazendo homing completo..."
        G28
    {% endif %}
    
    # Verificar se há filamento carregado
    {% if printer.extruder.temperature < 150 %}
        RESPOND MSG="⚠️ Aquecendo extrusor para verificações..."
        M104 S200
    {% endif %}
    
    # 2. Aquecimento inteligente
    RESPOND MSG="🔥 2/7 - Aquecimento inteligente..."
    {% set bed_temp = params.BED_TEMP|default(60)|int %}
    {% set extruder_temp = params.EXTRUDER_TEMP|default(200)|int %}
    
    M140 S{bed_temp}      # Iniciar aquecimento da mesa
    M104 S{extruder_temp} # Iniciar aquecimento do extrusor
    
    RESPOND MSG="🌡️ Aguardando temperaturas: Mesa {bed_temp}°C, Extrusor {extruder_temp}°C"
    M190 S{bed_temp}      # Aguardar mesa
    M109 S{extruder_temp} # Aguardar extrusor
    
    # 3. Nivelamento Z automático aprimorado
    RESPOND MSG="📐 3/7 - Nivelamento Z aprimorado..."
    Z_TILT_ADJUST_ENHANCED
    
    # 4. Bed mesh adaptativo
    RESPOND MSG="🗺️ 4/7 - Criando bed mesh adaptativo..."
    BED_MESH_CLEAR
    BED_MESH_CALIBRATE
    
    # 5. Calibração de tool offsets
    RESPOND MSG="🔧 5/7 - Calibrando tool offsets..."
    CAL_TOOLS
    
    # 6. Verificação de qualidade
    RESPOND MSG="✅ 6/7 - Verificação de qualidade..."
    VERIF_QUALIDADE
    
    # 7. Finalização e relatórios
    RESPOND MSG="📋 7/7 - Gerando relatórios finais..."
    REP_CAL
    
    # Salvar configuração
    SAVE_CONFIG
    
    RESPOND MSG="🎉 ===== CALIBRAÇÃO COMPLETA FINALIZADA ====="
    RESPOND MSG="✅ Impressora calibrada e pronta para uso!"
    RESPOND MSG="💾 Configurações salvas automaticamente"
    RESPOND MSG="🔄 Reinicializando para aplicar mudanças..."
    
    # Posição final segura
    G90
    G0 X150 Y100 Z50 F3000
    
    # Desligar aquecedores
    M104 S0
    M140 S0

[gcode_macro CAL_FAST]
description: Calibração rápida para uso diário (5-7 minutos)
gcode:
    RESPOND MSG="⚡ ===== CALIBRAÇÃO RÁPIDA ====="
    RESPOND MSG="⏱️ Tempo estimado: 5-7 minutos"
    
    # Homing se necessário
    {% if printer.toolhead.homed_axes != "xyz" %}
        G28
    {% endif %}
    
    # Aquecimento básico
    {% set bed_temp = params.BED_TEMP|default(60)|int %}
    M140 S{bed_temp}
    M190 S{bed_temp}
    
    # Z-Tilt rápido
    RESPOND MSG="📐 Nivelamento Z..."
    Z_TILT_ADJUST
    
    # Bed mesh rápido (3x3)
    RESPOND MSG="🗺️ Bed mesh rápido..."
    BED_MESH_CLEAR
    BED_MESH_CALIBRATE MESH_MIN=40,20 MESH_MAX=260,130 PROBE_COUNT=3,3
    
    # Verificação básica de offsets
    RESPOND MSG="🔧 Verificação de offsets..."
    {% for tool in range(1, 5) %}
        CAL_QUICK TOOL={tool}
    {% endfor %}
    
    RESPOND MSG="⚡ Calibração rápida concluída!"
    
    # Posição segura
    G0 X150 Y100 Z20 F3000
    M140 S0

# Variáveis globais para calibração
[gcode_macro CALIBRATION_DATA]
variable_reference_tool: 0                    # Ferramenta de referência (T0 - com BLTouch)
variable_calibration_points: []               # Pontos de medição
variable_temp_extruder: 200                   # Temperatura para calibração
variable_temp_bed: 60                         # Temperatura da mesa
variable_probe_speed: 5                       # Velocidade de probe
variable_probe_samples: 3                     # Amostras por ponto
variable_safe_z: 10                           # Altura segura
variable_calibration_active: False            # Status da calibração
variable_paper_thickness: 0.1                 # Espessura do papel (mm)
variable_nozzle_contact_offset: 0.0           # Offset de contato do bico
gcode:
    # Macro de dados - não executa código

#####################################################################
#                    CALIBRAÇÃO AUTOMÁTICA DE OFFSETS             #
#####################################################################

[gcode_macro CAL_TOOLS]
description: Calibração automática de offsets para todas as ferramentas
gcode:
    RESPOND MSG="🔧 Iniciando calibração automática de tool offsets..."
    
    # Verificações de segurança
    {% if not 'xyz' in printer.toolhead.homed_axes %}
        RESPOND MSG="⚠️ Fazendo homing completo..."
        G28
    {% endif %}
    
    # Marcar calibração como ativa
    SET_GCODE_VARIABLE MACRO=CALIBRATION_DATA VARIABLE=calibration_active VALUE=True
    
    # Aquecimento inteligente (mesa + extrusores para limpeza de bico)
    RESPOND MSG="🔥 Aquecendo para calibração..."
    M104 S{printer['gcode_macro CALIBRATION_DATA'].temp_extruder}  # Extrusor
    M140 S{printer['gcode_macro CALIBRATION_DATA'].temp_bed}       # Mesa
    
    # Aguardar temperaturas
    M109 S{printer['gcode_macro CALIBRATION_DATA'].temp_extruder}
    M190 S{printer['gcode_macro CALIBRATION_DATA'].temp_bed}
    
    # Nivelamento Z automático
    RESPOND MSG="📐 Executando nivelamento Z..."
    Z_TILT_ADJUST_ENHANCED
    
    # Calibrar cada ferramenta
    {% for tool_num in range(1, 5) %}  # T1 a T4
        {% if printer['gcode_macro TOOL_DATA']['tool_' ~ tool_num ~ '_enabled']|default(True) %}
            CAL_SINGLE TOOL={tool_num}
        {% endif %}
    {% endfor %}
    
    # Finalizar calibração
    SET_GCODE_VARIABLE MACRO=CALIBRATION_DATA VARIABLE=calibration_active VALUE=False
    
    # Relatório final
    REP_CAL
    
    RESPOND MSG="✅ Calibração automática concluída!"

#####################################################################
#                    CALIBRAÇÃO INDIVIDUAL POR FERRAMENTA         #
#####################################################################

[gcode_macro CAL_SINGLE]
description: Calibra offsets de uma ferramenta específica
gcode:
    {% set tool = params.TOOL|int %}
    {% set ref_tool = printer['gcode_macro CALIBRATION_DATA'].reference_tool|int %}
    
    RESPOND MSG="🔧 Calibrando T{tool}..."
    
    # Ativar ferramenta
    T{tool}
    
    # Aguardar estabilização
    G4 P2000
    
    # Definir pontos de medição (5 pontos para maior precisão)
    {% set points = [
        [50, 50],    # Canto inferior esquerdo
        [230, 50],   # Canto inferior direito
        [140, 100],  # Centro
        [50, 130],   # Canto superior esquerdo
        [230, 130]   # Canto superior direito
    ] %}
    
    {% set z_measurements = [] %}
    {% set x_positions = [] %}
    {% set y_positions = [] %}
    
    # Medir cada ponto
    {% for point in points %}
        {% set x = point[0] %}
        {% set y = point[1] %}
        
        RESPOND MSG="📍 Medindo ponto ({x}, {y})..."
        
        # Mover para posição segura
        G90
        G0 Z{printer['gcode_macro CALIBRATION_DATA'].safe_z} F600
        G0 X{x} Y{y} F3000
        
        # Fazer múltiplas medições para precisão
        {% set samples = [] %}
        {% for sample in range(printer['gcode_macro CALIBRATION_DATA'].probe_samples) %}
            # Usar BLTouch para T0 (referência) e sensor piezoelétrico para outras ferramentas
            {% if tool == ref_tool %}
                PROBE
                {% set z_result = printer.probe.last_z_result %}
            {% else %}
                # Usar sensor piezoelétrico para ferramentas T1-T3
                USE_PIEZO_PROBE
                PROBE
                {% set z_result = printer.probe.last_z_result|float %}
               # USE_BLTOUCH_PROBE
            {% endif %}
            {% set _ = samples.append(z_result) %}
        {% endfor %}
        
        # Calcular média das amostras
        {% set avg_z = (samples|sum / samples|length) %}
        {% set _ = z_measurements.append(avg_z) %}
        {% set _ = x_positions.append(x) %}
        {% set _ = y_positions.append(y) %}
        
        RESPOND MSG="{'📊 Z médio: ' ~ (avg_z|round(4)) ~ 'mm'}"
    {% endfor %}
    
    # Calcular offsets baseado na ferramenta de referência
    {% if tool != ref_tool %}
        CALC_OFS TOOL={tool} Z_MEASUREMENTS="{z_measurements|join(',')}"
    {% else %}
        RESPOND MSG="{'📌 T' ~ tool ~ ' é a ferramenta de referência'}"
        # Salvar medições de referência
        SAVE_VARIABLE VARIABLE=ref_z_measurements VALUE="{z_measurements|join(',')}"
    {% endif %}
    
    # Retornar à altura segura
    G0 Z{printer['gcode_macro CALIBRATION_DATA'].safe_z} F600

#####################################################################
#                    CÁLCULO DE OFFSETS                           #
#####################################################################

[gcode_macro CALC_OFS]
description: Calcula e aplica offsets baseado nas medições
gcode:
    {% set tool = params.TOOL|int %}
    {% set measurements = params.Z_MEASUREMENTS.split(',') %}
    {% set ref_tool = printer['gcode_macro CALIBRATION_DATA'].reference_tool|int %}
    
    # Obter medições de referência
    {% if 'ref_z_measurements' in printer.save_variables.variables %}
        {% set ref_measurements = printer.save_variables.variables.ref_z_measurements.split(',') %}
    {% else %}
        RESPOND MSG="❌ Erro: Medições de referência não encontradas. Execute primeiro a calibração da ferramenta de referência."
        {% set ref_measurements = [] %}
    {% endif %}
    
    # Calcular diferenças Z
    {% if ref_measurements|length > 0 %}
        {% set z_diffs = [] %}
        {% for i in range(measurements|length) %}
            {% if i < ref_measurements|length %}
                {% set diff = measurements[i]|float - ref_measurements[i]|float %}
                {% set _ = z_diffs.append(diff) %}
            {% endif %}
        {% endfor %}
        
        # Calcular offset Z médio
        {% if z_diffs|length > 0 %}
            {% set z_offset = (z_diffs|sum / z_diffs|length) %}
        {% else %}
            {% set z_offset = 0.0 %}
            RESPOND MSG="❌ Erro: Não foi possível calcular offset - dados insuficientes."
        {% endif %}
    {% else %}
        {% set z_offset = 0.0 %}
        RESPOND MSG="❌ Erro: Medições de referência vazias. Calibração cancelada."
    {% endif %}
    
    RESPOND MSG="{'📊 T' ~ tool ~ ' - Offset Z calculado: ' ~ (z_offset|round(4)) ~ 'mm'}"
    
    # Aplicar offset calculado
    {% set current_z_offset = printer['gcode_macro TOOL_DATA']['offset_z' ~ tool]|default(0.0)|float %}
    {% set new_z_offset = current_z_offset + z_offset %}
    
    # Salvar novo offset
    SET_GCODE_VARIABLE MACRO=TOOL_DATA VARIABLE=offset_z{tool} VALUE={new_z_offset}
    SAVE_VARIABLE VARIABLE=tool_{tool}_offset_z VALUE={new_z_offset}
    
    RESPOND MSG="{'✅ T' ~ tool ~ ' - Novo offset Z: ' ~ (new_z_offset|round(4)) ~ 'mm'}"
    
    # Verificar se offset está dentro de limites aceitáveis
    {% if z_offset|abs > 0.5 %}
        RESPOND MSG="{'⚠️ ATENÇÃO: Offset Z muito grande (' ~ (z_offset|round(4)) ~ 'mm) - Verificar hardware!'}"
    {% elif z_offset|abs > 0.2 %}
        RESPOND MSG="{'⚠️ Offset Z significativo (' ~ (z_offset|round(4)) ~ 'mm) - Monitorar'}"
    {% else %}
        RESPOND MSG="{'✅ Offset Z dentro do normal (' ~ (z_offset|round(4)) ~ 'mm)'}"
    {% endif %}

#####################################################################
#                    CALIBRAÇÃO RÁPIDA (SINGLE POINT)             #
#####################################################################

[gcode_macro CAL_QUICK]
description: Calibração rápida de offset Z para uma ferramenta
gcode:
    {% set tool = params.TOOL|int %}
    {% set ref_tool = printer['gcode_macro CALIBRATION_DATA'].reference_tool|int %}
    
    RESPOND MSG="⚡ Calibração rápida T{tool}..."
    
    # Verificar homing
    {% if not 'xyz' in printer.toolhead.homed_axes %}
        G28
    {% endif %}
    
    # Ponto central para medição rápida
    {% set x_center = 140 %}
    {% set y_center = 100 %}
    
    # Medir ferramenta de referência (sempre com BLTouch)
    T{ref_tool}
    G4 P1000
    G90
    G0 Z10 F600
    G0 X{x_center} Y{y_center} F3000
    G30
    {% set ref_z = printer.probe.last_z_result %}
    
    # Medir ferramenta alvo
    T{tool}
    G4 P1000
    {% if tool == ref_tool %}
        # Usar BLTouch para ferramenta de referência
        G30
        {% set tool_z = printer.probe.last_z_result %}
    {% else %}
        # Usar sensor piezoelétrico para outras ferramentas
        # Alternar para probe piezoelétrico
        USE_PIEZO_PROBE
        # Mover para posição do sensor piezoelétrico
        G0 X150 Y150 F3000
        PROBE
        {% set tool_z = printer.probe.last_z_result|float %}
        # Voltar para BLTouch
        #USE_BLTOUCH_PROBE
    {% endif %}
    
    # Calcular e aplicar offset
    {% set z_diff = tool_z - ref_z %}
    {% set current_offset = printer['gcode_macro TOOL_DATA']['offset_z' ~ tool]|default(0.0)|float %}
    {% set new_offset = current_offset + z_diff %}
    
    SET_GCODE_VARIABLE MACRO=TOOL_DATA VARIABLE=offset_z{tool} VALUE={new_offset}
    SAVE_VARIABLE VARIABLE=tool_{tool}_offset_z VALUE={new_offset}
    
    RESPOND MSG="{'⚡ T' ~ tool ~ ' - Offset rápido: ' ~ (z_diff|round(4)) ~ 'mm (Total: ' ~ (new_offset|round(4)) ~ 'mm)'}"
    
    G0 Z10 F600

#####################################################################
#                    RELATÓRIO DE CALIBRAÇÃO                      #
#####################################################################

[gcode_macro REP_CAL]
description: Gera relatório detalhado da calibração
gcode:
    RESPOND MSG="📋 ===== RELATÓRIO DE CALIBRAÇÃO ====="
    RESPOND MSG="📅 Data: {printer.system_stats.cputime|int}"
    RESPOND MSG="🌡️ Temp. Extrusor: {printer['gcode_macro CALIBRATION_DATA'].temp_extruder}°C"
    RESPOND MSG="🌡️ Temp. Mesa: {printer['gcode_macro CALIBRATION_DATA'].temp_bed}°C"
    RESPOND MSG="📐 Ferramenta Referência: T{printer['gcode_macro CALIBRATION_DATA'].reference_tool}"
    RESPOND MSG=""
    RESPOND MSG="📊 OFFSETS ATUAIS:"
    
    {% for tool_num in range(1, 5) %}
        {% set z_offset = printer['gcode_macro TOOL_DATA']['offset_z' ~ tool_num]|default(0.0)|float %}
        {% set x_offset = printer['gcode_macro TOOL_DATA']['offset_x' ~ tool_num]|default(0.0)|float %}
        {% set y_offset = printer['gcode_macro TOOL_DATA']['offset_y' ~ tool_num]|default(0.0)|float %}
        
        RESPOND MSG="{'🔧 T' ~ tool_num ~ ': X=' ~ (x_offset|round(3)) ~ ' Y=' ~ (y_offset|round(3)) ~ ' Z=' ~ (z_offset|round(4)) ~ ''}"
    {% endfor %}
    
    RESPOND MSG=""
    RESPOND MSG="✅ Calibração concluída com sucesso!"
    RESPOND MSG="💾 Offsets salvos automaticamente"
    RESPOND MSG="🔄 Use RESTART para aplicar mudanças"

#####################################################################
#                    UTILITÁRIOS DE CALIBRAÇÃO                    #
#####################################################################

[gcode_macro RST_OFS]
description: Reseta todos os offsets para zero
gcode:
    RESPOND MSG="🔄 Resetando todos os tool offsets..."
    
    {% for tool_num in range(1, 5) %}
        SET_GCODE_VARIABLE MACRO=TOOL_DATA VARIABLE=offset_x{tool_num} VALUE=0.0
        SET_GCODE_VARIABLE MACRO=TOOL_DATA VARIABLE=offset_y{tool_num} VALUE=0.0
        SET_GCODE_VARIABLE MACRO=TOOL_DATA VARIABLE=offset_z{tool_num} VALUE=0.0
        SAVE_VARIABLE VARIABLE=tool_{tool_num}_offset_x VALUE=0.0
        SAVE_VARIABLE VARIABLE=tool_{tool_num}_offset_y VALUE=0.0
        SAVE_VARIABLE VARIABLE=tool_{tool_num}_offset_z VALUE=0.0
    {% endfor %}
    
    RESPOND MSG="✅ Todos os offsets resetados para 0.0"
    RESPOND MSG="🔄 Use RESTART para aplicar mudanças"

[gcode_macro BAK_OFS]
description: Faz backup dos offsets atuais
gcode:
    {% set timestamp = printer.system_stats.cputime|int %}
    
    RESPOND MSG="💾 Fazendo backup dos offsets..."
    
    {% for tool_num in range(1, 5) %}
        {% set z_offset = printer['gcode_macro TOOL_DATA']['offset_z' ~ tool_num]|default(0.0)|float %}
        {% set x_offset = printer['gcode_macro TOOL_DATA']['offset_x' ~ tool_num]|default(0.0)|float %}
        {% set y_offset = printer['gcode_macro TOOL_DATA']['offset_y' ~ tool_num]|default(0.0)|float %}
        
        SAVE_VARIABLE VARIABLE=backup_{timestamp}_tool_{tool_num}_x VALUE={x_offset}
        SAVE_VARIABLE VARIABLE=backup_{timestamp}_tool_{tool_num}_y VALUE={y_offset}
        SAVE_VARIABLE VARIABLE=backup_{timestamp}_tool_{tool_num}_z VALUE={z_offset}
    {% endfor %}
    
    SAVE_VARIABLE VARIABLE=last_backup_timestamp VALUE={timestamp}
    
    RESPOND MSG="✅ Backup salvo com timestamp: {timestamp}"

[gcode_macro STS_CAL]
description: Mostra status atual da calibração
gcode:
    RESPOND MSG="📊 ===== STATUS DE CALIBRAÇÃO ====="
    RESPOND MSG="🔧 Calibração ativa: {printer['gcode_macro CALIBRATION_DATA'].calibration_active}"
    RESPOND MSG="📐 Ferramenta referência: T{printer['gcode_macro CALIBRATION_DATA'].reference_tool}"
    RESPOND MSG="🌡️ Temp. calibração: {printer['gcode_macro CALIBRATION_DATA'].temp_extruder}°C"
    RESPOND MSG="📏 Amostras por ponto: {printer['gcode_macro CALIBRATION_DATA'].probe_samples}"
    RESPOND MSG="⚡ Velocidade probe: {printer['gcode_macro CALIBRATION_DATA'].probe_speed}mm/s"
    
    {% if printer.save_variables.variables.last_backup_timestamp is defined %}
        RESPOND MSG="💾 Último backup: {printer.save_variables.variables.last_backup_timestamp}"
    {% else %}
        RESPOND MSG="💾 Nenhum backup encontrado"
    {% endif %}

#####################################################################
#                    VERIFICAÇÃO DE QUALIDADE                     #
#####################################################################

[gcode_macro VERIF_QUALIDADE]
description: Verificação completa de qualidade pós-calibração
gcode:
    RESPOND MSG="🔍 ===== VERIFICAÇÃO DE QUALIDADE ====="
    
    # 1. Verificar precisão do nivelamento Z
    RESPOND MSG="📐 Verificando precisão do nivelamento Z..."
    CHK_LVL
    
    # 2. Verificar consistência dos offsets
    RESPOND MSG="🔧 Verificando consistência dos tool offsets..."
    VERIF_OFFSETS
    
    # 3. Verificar qualidade do bed mesh
    RESPOND MSG="🗺️ Verificando qualidade do bed mesh..."
    VERIFICAR_QUALIDADE_MESH
    
    # 4. Teste de primeira camada simulado
    RESPOND MSG="📏 Simulando teste de primeira camada..."
    TESTE_CAMADA
    
    RESPOND MSG="✅ Verificação de qualidade concluída!"

[gcode_macro CHK_LVL]
description: Verifica a precisão do nivelamento Z
gcode:
    # Medir pontos de nivelamento novamente
    G90
    G0 Z10 F600
    
    # Ponto esquerdo
    G0 X0 Y100 F3000
    PROBE
    {% set z_left = printer.probe.last_z_result %}
    
    # Ponto direito
    G0 X260 Y100 F3000
    PROBE
    {% set z_right = printer.probe.last_z_result %}
    
    # Calcular diferença
    {% set diff = (z_left - z_right)|abs %}
    
    {% if diff <= 0.02 %}
        RESPOND MSG="{'✅ Nivelamento Z excelente: ' ~ (diff|round(4)) ~ 'mm'}"
    {% elif diff <= 0.05 %}
        RESPOND MSG="{'⚠️ Nivelamento Z aceitável: ' ~ (diff|round(4)) ~ 'mm'}"
    {% else %}
        RESPOND MSG="{'❌ Nivelamento Z problemático: ' ~ (diff|round(4)) ~ 'mm - Requer atenção!'}"
    {% endif %}
    
    G0 Z10 F600

[gcode_macro VERIF_OFFSETS]
description: Verifica se os offsets estão dentro de limites aceitáveis
gcode:
    {% set problemas = [] %}
    
    {% for tool_num in range(1, 5) %}
        {% set z_offset = printer['gcode_macro TOOL_DATA']['offset_z' ~ tool_num]|default(0.0)|float %}
        {% set x_offset = printer['gcode_macro TOOL_DATA']['offset_x' ~ tool_num]|default(0.0)|float %}
        {% set y_offset = printer['gcode_macro TOOL_DATA']['offset_y' ~ tool_num]|default(0.0)|float %}
        
        # Verificar limites Z (±0.5mm)
        {% if z_offset|abs > 0.5 %}
            {% set _ = problemas.append("T" ~ tool_num ~ " Z offset muito grande: " ~ z_offset ~ "mm") %}
        {% endif %}
        
        # Verificar limites X/Y (±2.0mm)
        {% if x_offset|abs > 2.0 %}
            {% set _ = problemas.append("T" ~ tool_num ~ " X offset muito grande: " ~ x_offset ~ "mm") %}
        {% endif %}
        
        {% if y_offset|abs > 2.0 %}
            {% set _ = problemas.append("T" ~ tool_num ~ " Y offset muito grande: " ~ y_offset ~ "mm") %}
        {% endif %}
    {% endfor %}
    
    {% if problemas|length == 0 %}
        RESPOND MSG="✅ Todos os offsets estão dentro dos limites aceitáveis"
    {% else %}
        RESPOND MSG="⚠️ Problemas detectados nos offsets:"
        {% for problema in problemas %}
            RESPOND MSG="   - {problema}"
        {% endfor %}
    {% endif %}

[gcode_macro VERIFICAR_QUALIDADE_MESH]
description: Analisa a qualidade do bed mesh
gcode:
    {% if printer.bed_mesh.mesh_matrix %}
        {% set mesh = printer.bed_mesh.mesh_matrix %}
        {% set min_val = mesh|map('min')|min %}
        {% set max_val = mesh|map('max')|max %}
        {% set range_val = max_val - min_val %}
        
        RESPOND MSG="📊 Análise do Bed Mesh:"
        RESPOND MSG="{'   Valor mínimo: ' ~ (min_val|round(4)) ~ 'mm'}"
        RESPOND MSG="{'   Valor máximo: ' ~ (max_val|round(4)) ~ 'mm'}"
        RESPOND MSG="{'   Variação total: ' ~ (range_val|round(4)) ~ 'mm'}"
        
        {% if range_val <= 0.1 %}
            RESPOND MSG="✅ Mesa muito plana (variação ≤ 0.1mm)"
        {% elif range_val <= 0.2 %}
            RESPOND MSG="✅ Mesa boa (variação ≤ 0.2mm)"
        {% elif range_val <= 0.5 %}
            RESPOND MSG="⚠️ Mesa aceitável (variação ≤ 0.5mm)"
        {% else %}
            RESPOND MSG="❌ Mesa irregular (variação > 0.5mm) - Considerar ajustes mecânicos"
        {% endif %}
    {% else %}
        RESPOND MSG="❌ Nenhum bed mesh ativo encontrado"
    {% endif %}

[gcode_macro TESTE_CAMADA]
description: Simula teste de primeira camada em pontos críticos
gcode:
    RESPOND MSG="📏 Testando altura de primeira camada..."
    
    # Pontos críticos para teste
    {% set test_points = [
        [50, 50, "Canto inferior esquerdo"],
        [230, 50, "Canto inferior direito"],
        [140, 100, "Centro"],
        [50, 130, "Canto superior esquerdo"],
        [230, 130, "Canto superior direito"]
    ] %}
    
    {% set problemas = [] %}
    
    {% for point in test_points %}
        {% set x = point[0] %}
        {% set y = point[1] %}
        {% set desc = point[2] %}
        
        # Mover para ponto
        G90
        G0 Z5 F600
        G0 X{x} Y{y} F3000
        
        # Simular altura de primeira camada (0.2mm)
        G0 Z0.2 F300
        G4 P500  # Pausa para "simular" extrusão
        
        # Verificar se a altura é segura
        {% set current_z = printer.toolhead.position.z %}
        {% if current_z < 0.15 or current_z > 0.25 %}
            {% set _ = problemas.append(desc ~ ": altura suspeita (" ~ current_z ~ "mm)") %}
        {% endif %}
    {% endfor %}
    
    {% if problemas|length == 0 %}
        RESPOND MSG="✅ Simulação de primeira camada: todas as alturas OK"
    {% else %}
        RESPOND MSG="⚠️ Problemas detectados na simulação:"
        {% for problema in problemas %}
            RESPOND MSG="   - {problema}"
        {% endfor %}
    {% endif %}
    
    # Retornar à altura segura
    G0 Z10 F600

[gcode_macro RELAT_CALIB]
description: Relatório detalhado da calibração completa
gcode:
    RESPOND MSG="📋 ===== RELATÓRIO COMPLETO DE CALIBRAÇÃO ====="
    RESPOND MSG="📅 Data/Hora: {printer.system_stats.cputime|int}"
    RESPOND MSG="🖨️ Impressora: Multi-Tool CoreXY"
    RESPOND MSG=""    
    # Informações de temperatura
    RESPOND MSG="🌡️ TEMPERATURAS DE CALIBRAÇÃO:"
    RESPOND MSG="{'   Mesa: ' ~ printer.heater_bed.target ~ '°C (atual: ' ~ (printer.heater_bed.temperature|round(1)) ~ '°C)'}"
    RESPOND MSG="{'   Extrusor: ' ~ printer.extruder.target ~ '°C (atual: ' ~ (printer.extruder.temperature|round(1)) ~ '°C)'}"
    RESPOND MSG=""    
    # Status do nivelamento
    RESPOND MSG="📐 NIVELAMENTO Z:"
    RESPOND MSG="   Retries configurados: {printer.configfile.settings.z_tilt.retries}"
    RESPOND MSG="   Tolerância: {printer.configfile.settings.z_tilt.retry_tolerance}mm"
    RESPOND MSG="   Status: ✅ Concluído com sucesso"
    RESPOND MSG=""    
    # Bed mesh
    RESPOND MSG="🗺️ BED MESH:"
    {% if printer.bed_mesh.mesh_matrix %}
        {% set mesh = printer.bed_mesh.mesh_matrix %}
        {% set min_val = mesh|map('min')|min %}
        {% set max_val = mesh|map('max')|max %}
        RESPOND MSG="{'   Pontos: ' ~ mesh|length ~ 'x' ~ mesh[0]|length}"
        RESPOND MSG="{'   Variação: ' ~ ((max_val - min_val)|round(4)) ~ 'mm'}"
        RESPOND MSG="   Status: ✅ Ativo"
    {% else %}
        RESPOND MSG="   Status: ❌ Não encontrado"
    {% endif %}
    RESPOND MSG=""    
    # Tool offsets
    RESPOND MSG="🔧 TOOL OFFSETS FINAIS:"
    {% for tool_num in range(1, 5) %}
        {% set z_offset = printer['gcode_macro TOOL_DATA']['offset_z' ~ tool_num]|default(0.0)|float %}
        {% set x_offset = printer['gcode_macro TOOL_DATA']['offset_x' ~ tool_num]|default(0.0)|float %}
        {% set y_offset = printer['gcode_macro TOOL_DATA']['offset_y' ~ tool_num]|default(0.0)|float %}
        
        RESPOND MSG="{'   T' ~ tool_num ~ ': X=' ~ ('%+.3f'|format(x_offset)) ~ ' Y=' ~ ('%+.3f'|format(y_offset)) ~ ' Z=' ~ ('%+.4f'|format(z_offset)) ~ 'mm'}"
    {% endfor %}
    RESPOND MSG=""    
    # Recomendações
    RESPOND MSG="💡 RECOMENDAÇÕES:"
    RESPOND MSG="   ✅ Execute CALIBRACAO_RAPIDA antes de impressões importantes"
    RESPOND MSG="   ✅ Monitore a primeira camada de cada impressão"
    RESPOND MSG="   ✅ Refaça calibração completa a cada 50-100 horas de uso"
    RESPOND MSG="   ✅ Use BACKUP_OFFSETS antes de grandes mudanças"
    RESPOND MSG=""    
    RESPOND MSG="🎯 CALIBRAÇÃO COMPLETA FINALIZADA COM SUCESSO!"
    RESPOND MSG="💾 Todas as configurações foram salvas automaticamente"

#####################################################################
#           CALIBRAÇÃO ESPECIALIZADA PARA BLTOUCH FIXO            #
#####################################################################

[gcode_macro CALIB_Z_BLT]
description: Calibração de Z offsets para configuração com BLTouch fixo no T0
gcode:
    RESPOND MSG="🎯 ===== CALIBRAÇÃO Z OFFSETS - BLTOUCH FIXO ====="
    RESPOND MSG="📌 T0 é a referência (BLTouch fixo)"
    RESPOND MSG="🔧 Calibrando T1, T2, T3 relativos ao T0"
    
    # Verificações iniciais
    {% if not 'xyz' in printer.toolhead.homed_axes %}
        RESPOND MSG="⚠️ Fazendo homing completo..."
        G28
    {% endif %}
    
    # Aquecimento completo (mesa + extrusores para limpeza de bico)
    RESPOND MSG="🔥 Aquecendo para calibração..."
    M104 S{printer['gcode_macro CALIBRATION_DATA'].temp_extruder}
    M140 S{printer['gcode_macro CALIBRATION_DATA'].temp_bed}
    M109 S{printer['gcode_macro CALIBRATION_DATA'].temp_extruder}
    M190 S{printer['gcode_macro CALIBRATION_DATA'].temp_bed}
    
    # Nivelamento Z
    RESPOND MSG="📐 Nivelamento Z..."
    Z_TILT_ADJUST_ENHANCED
    
    # Estabelecer referência T0 com BLTouch
    RESPOND MSG="📍 Estabelecendo referência T0 (BLTouch)..."
    ESTABELECER_REFERENCIA_T0
    
    # Calibrar primeira ferramenta (T1)
    CALIBRAR_TOOL_MANUAL_Z TOOL=1
    
    RESPOND MSG="⏸️ CALIBRAÇÃO PAUSADA"
    RESPOND MSG="📋 PRÓXIMOS PASSOS:"
    RESPOND MSG="   1. Ajuste T1 com os comandos mostrados"
     RESPOND MSG="   2. Execute CONFIRMAR_Z_T1 quando perfeito"
     RESPOND MSG="   3. Execute CALIB_PROX para T2"
     RESPOND MSG="   4. Execute RELAT_Z_FINAL quando terminar"
     RESPOND MSG=""
     RESPOND MSG="✅ Calibração iniciada com sucesso!"
     RESPOND MSG="💡 Agora você tem controle total do processo"

[gcode_macro CALIB_PROX]
description: Continua calibração para a próxima ferramenta
gcode:
    {% set current_tool = printer.save_variables.variables.current_calibrating_tool|default(1)|int %}
    {% set next_tool = current_tool + 1 %}
    
    {% if next_tool <= 2 %}  # Apenas T1 e T2 (T3 desabilitado no tools.cfg)
        CALIBRAR_TOOL_MANUAL_Z TOOL={next_tool}
        
        RESPOND MSG="⏸️ CALIBRAÇÃO PAUSADA"
        RESPOND MSG="📋 PRÓXIMOS PASSOS:"
        RESPOND MSG="{'   1. Ajuste T' ~ next_tool ~ ' com os comandos mostrados'}"
        RESPOND MSG="{'   2. Execute CONFIRMAR_Z_T' ~ next_tool ~ ' quando perfeito'}"
        {% if next_tool < 2 %}
            RESPOND MSG="   3. Execute CALIBRAR_PROXIMA_FERRAMENTA para continuar"
        {% else %}
            RESPOND MSG="   3. Execute RELATORIO_Z_OFFSETS_FINAL quando terminar"
        {% endif %}
    {% else %}
        RESPOND MSG="✅ Todas as ferramentas foram calibradas!"
        RESPOND MSG="💡 Execute RELATORIO_Z_OFFSETS_FINAL para ver o resultado"
    {% endif %}

[gcode_macro ESTABELECER_REFERENCIA_T0]
description: Estabelece T0 como referência usando BLTouch
gcode:
    RESPOND MSG="📍 Ativando T0 (referência com BLTouch)..."
    
    # Ativar T0
    T0
    G4 P2000  # Aguardar estabilização
    
    # Ir para centro da mesa
    G90
    G0 Z10 F600
    G0 X140 Y100 F3000
    
    # Fazer medição de referência com BLTouch
    RESPOND MSG="🔍 Medindo altura de referência com BLTouch..."
    #BLTOUCH_DEBUG COMMAND=pin_down  # Garantir deploy
    G4 P500  # Aguardar deploy
    PROBE  # Probe com BLTouch
    {% set ref_z = printer.probe.last_z_result %}
    G0 Z10 F600  # Subir Z imediatamente após probe
#BLTOUCH_DEBUG COMMAND=pin_up    # Garantir retração
    G4 P500  # Aguardar retração
    
    # Salvar como referência
    SAVE_VARIABLE VARIABLE=t0_reference_z VALUE={ref_z}
    
    RESPOND MSG="{'✅ T0 referência estabelecida: Z = ' ~ (ref_z|round(4)) ~ 'mm'}"
    
    # Posição segura
    G0 Z15 F600  # Altura segura final

[gcode_macro CALIBRAR_TOOL_MANUAL_Z]
description: Calibração manual de Z offset para uma ferramenta específica
gcode:
    {% set tool = params.TOOL|int %}
    {% set ref_z = printer.save_variables.variables.t0_reference_z|float %}
    
    RESPOND MSG="🔧 ===== CALIBRANDO T{tool} ====="
    RESPOND MSG="📋 INSTRUÇÕES:"
    RESPOND MSG="   1. Coloque papel entre bico e mesa"
    RESPOND MSG="   2. Use os comandos de movimento para ajustar"
    RESPOND MSG="   3. Quando o papel arrastar levemente, confirme"
    
    # Ativar ferramenta
    T{tool}
    G4 P2000  # Aguardar estabilização
    
    # Ir para centro da mesa
    G90
    G0 Z10 F600
    G0 X140 Y100 F3000
    
    # Descer para altura próxima da mesa (baseado na referência T0)
    {% set start_z = ref_z + 0.5 %}  # 0.5mm acima da referência
    G0 Z{start_z} F300
    
    RESPOND MSG="{'🎯 T' ~ tool ~ ' posicionado em Z = ' ~ (start_z|round(3)) ~ 'mm'}"
    RESPOND MSG="📄 Coloque papel e use os comandos:"
    RESPOND MSG="   AJUSTAR_Z_BAIXO_MEDIO (desce 0.1mm)"
    RESPOND MSG="   AJUSTAR_Z_CIMA_MEDIO (sobe 0.1mm)"
    RESPOND MSG="   AJUSTAR_Z_BAIXO (desce 0.01mm)"
    RESPOND MSG="   AJUSTAR_Z_CIMA (sobe 0.01mm)"
    RESPOND MSG="   AJUSTAR_Z_BAIXO_FINO (desce 0.002mm)"
    RESPOND MSG="   AJUSTAR_Z_CIMA_FINO (sobe 0.002mm)"
    RESPOND MSG="{'   CONFIRMAR_Z_T' ~ tool ~ ' (quando estiver perfeito)'}"
    
    # Salvar estado atual para esta ferramenta
    SET_GCODE_VARIABLE MACRO=CALIBRATION_DATA VARIABLE=calibration_active VALUE=True
    SAVE_VARIABLE VARIABLE=current_calibrating_tool VALUE={tool}
    SAVE_VARIABLE VARIABLE=current_z_position VALUE={start_z}

[gcode_macro AJUSTAR_Z_BAIXO]
description: Desce Z em 0.01mm durante calibração
gcode:
    {% if printer['gcode_macro CALIBRATION_DATA'].calibration_active %}
        {% set current_z = printer.save_variables.variables.current_z_position|float %}
        {% set new_z = current_z - 0.01 %}
        G90
        G0 Z{new_z} F100
        SAVE_VARIABLE VARIABLE=current_z_position VALUE={new_z}
        RESPOND MSG="{'⬇️ Z = ' ~ (new_z|round(3)) ~ 'mm'}"
    {% else %}
        RESPOND MSG="❌ Nenhuma calibração ativa"
    {% endif %}

[gcode_macro AJUSTAR_Z_CIMA]
description: Sobe Z em 0.01mm durante calibração
gcode:
    {% if printer['gcode_macro CALIBRATION_DATA'].calibration_active %}
        {% set current_z = printer.save_variables.variables.current_z_position|float %}
        {% set new_z = current_z + 0.01 %}
        G90
        G0 Z{new_z} F100
        SAVE_VARIABLE VARIABLE=current_z_position VALUE={new_z}
        RESPOND MSG="{'⬆️ Z = ' ~ (new_z|round(3)) ~ 'mm'}"
    {% else %}
        RESPOND MSG="❌ Nenhuma calibração ativa"
    {% endif %}

[gcode_macro AJUSTAR_Z_BAIXO_FINO]
description: Desce Z em 0.002mm durante calibração (ajuste fino)
gcode:
    {% if printer['gcode_macro CALIBRATION_DATA'].calibration_active %}
        {% set current_z = printer.save_variables.variables.current_z_position|float %}
        {% set new_z = current_z - 0.002 %}
        G90
        G0 Z{new_z} F50
        SAVE_VARIABLE VARIABLE=current_z_position VALUE={new_z}
        RESPOND MSG="{'⬇️🔍 Z = ' ~ (new_z|round(4)) ~ 'mm (fino)'}"
    {% else %}
        RESPOND MSG="❌ Nenhuma calibração ativa"
    {% endif %}

[gcode_macro AJUSTAR_Z_CIMA_FINO]
description: Sobe Z em 0.002mm durante calibração (ajuste fino)
gcode:
    {% if printer['gcode_macro CALIBRATION_DATA'].calibration_active %}
        {% set current_z = printer.save_variables.variables.current_z_position|float %}
        {% set new_z = current_z + 0.002 %}
        G90
        G0 Z{new_z} F50
        SAVE_VARIABLE VARIABLE=current_z_position VALUE={new_z}
        RESPOND MSG="{'⬆️🔍 Z = ' ~ (new_z|round(4)) ~ 'mm (fino)'}"
    {% else %}
        RESPOND MSG="❌ Nenhuma calibração ativa"
    {% endif %}

[gcode_macro AJUSTAR_Z_BAIXO_MEDIO]
description: Desce Z em 0.1mm durante calibração (ajuste médio)
gcode:
    {% if printer['gcode_macro CALIBRATION_DATA'].calibration_active %}
        {% set current_z = printer.save_variables.variables.current_z_position|float %}
        {% set new_z = current_z - 0.1 %}
        G90
        G0 Z{new_z} F200
        SAVE_VARIABLE VARIABLE=current_z_position VALUE={new_z}
        RESPOND MSG="{'⬇️📏 Z = ' ~ (new_z|round(3)) ~ 'mm (médio)'}"
    {% else %}
        RESPOND MSG="❌ Nenhuma calibração ativa"
    {% endif %}

[gcode_macro AJUSTAR_Z_CIMA_MEDIO]
description: Sobe Z em 0.1mm durante calibração (ajuste médio)
gcode:
    {% if printer['gcode_macro CALIBRATION_DATA'].calibration_active %}
        {% set current_z = printer.save_variables.variables.current_z_position|float %}
        {% set new_z = current_z + 0.1 %}
        G90
        G0 Z{new_z} F200
        SAVE_VARIABLE VARIABLE=current_z_position VALUE={new_z}
        RESPOND MSG="{'⬆️📏 Z = ' ~ (new_z|round(3)) ~ 'mm (médio)'}"
    {% else %}
        RESPOND MSG="❌ Nenhuma calibração ativa"
    {% endif %}

[gcode_macro CALIB_OFFS]
description: Macro completa para calibração de offsets com PLA
gcode:
    RESPOND MSG="🚀 ===== INICIANDO CALIBRAÇÃO COMPLETA ====="
    RESPOND MSG="🔥 Configurando para PLA..."
    
    # Configurar temperaturas para PLA
    PLA
    
    RESPOND MSG="🏠 Fazendo homing completo..."
    # Homing completo
    G28
    
    RESPOND MSG="🔧 Ativando ferramenta T0..."
    # Ativar T0
    T0
    
    RESPOND MSG="📐 Iniciando calibração de Z offsets..."
    # Executar calibração de offsets
    CALIB_Z_BLT
    
    RESPOND MSG="✅ Sequência de calibração iniciada com sucesso!"
    RESPOND MSG="💡 Continue seguindo as instruções da calibração"

[gcode_macro CONFIRMAR_Z_T1]
description: Confirma calibração da T1
gcode:
    CONFIRMAR_Z_TOOL TOOL=1

[gcode_macro CONFIRMAR_Z_T2]
description: Confirma calibração da T2
gcode:
    CONFIRMAR_Z_TOOL TOOL=2

[gcode_macro CONFIRMAR_Z_T3]
description: Confirma calibração da T3
gcode:
    CONFIRMAR_Z_TOOL TOOL=3

[gcode_macro CONFIRMAR_Z_TOOL]
description: Confirma calibração de uma ferramenta específica
gcode:
    {% set tool = params.TOOL|int %}
    {% set current_tool = printer.save_variables.variables.current_calibrating_tool|int %}
    
    {% if tool != current_tool %}
        RESPOND MSG="❌ Ferramenta incorreta! Calibrando T{current_tool}, mas confirmando T{tool}"
        RESPOND MSG="💡 Use CONFIRMAR_Z_T{current_tool}"
    {% else %}
        {% set final_z = printer.save_variables.variables.current_z_position|float %}
        {% set ref_z = printer.save_variables.variables.t0_reference_z|float %}
        {% set paper_thickness = printer['gcode_macro CALIBRATION_DATA'].paper_thickness|float %}
        
        # Calcular offset Z (diferença em relação ao T0, compensando papel)
        {% set z_offset = (final_z - ref_z) - paper_thickness %}
        
        # Salvar offset
        SET_GCODE_VARIABLE MACRO=TOOL_DATA VARIABLE=offset_z{tool} VALUE={z_offset}
        SAVE_VARIABLE VARIABLE=tool_{tool}_offset_z VALUE={z_offset}
        
        RESPOND MSG="{'✅ T' ~ tool ~ ' calibrado com sucesso!'}"
        RESPOND MSG="{'📊 Altura final: ' ~ (final_z|round(4)) ~ 'mm'}"
        RESPOND MSG="{'📊 Referência T0: ' ~ (ref_z|round(4)) ~ 'mm'}"
        RESPOND MSG="{'📊 Z Offset T' ~ tool ~ ': ' ~ (z_offset|round(4)) ~ 'mm'}"
        
        # Verificar se offset está razoável
        {% if z_offset|abs > 1.0 %}
            RESPOND MSG="{'⚠️ ATENÇÃO: Offset muito grande (' ~ (z_offset|round(4)) ~ 'mm)!'}"
            RESPOND MSG="💡 Verifique se o papel foi usado corretamente"
        {% elif z_offset|abs > 0.5 %}
            RESPOND MSG="{'⚠️ Offset significativo (' ~ (z_offset|round(4)) ~ 'mm) - Verificar'}"
        {% else %}
            RESPOND MSG="{'✅ Offset dentro do normal (' ~ (z_offset|round(4)) ~ 'mm)'}"
        {% endif %}
        
        # Ir para altura segura
        G0 Z10 F600
        
        # Manter calibração ativa para próxima ferramenta
        # SET_GCODE_VARIABLE MACRO=CALIBRATION_DATA VARIABLE=calibration_active VALUE=False
        
        RESPOND MSG="✅ T{tool} calibrado com sucesso!"
        RESPOND MSG="🎯 Execute CALIBRAR_PROXIMA_FERRAMENTA para continuar"
        RESPOND MSG="💡 Ou execute RELATORIO_Z_OFFSETS_FINAL se terminou"
    {% endif %}

[gcode_macro RELAT_Z_FINAL]
description: Relatório final dos Z offsets calibrados
gcode:
    # Finalizar calibração
    SET_GCODE_VARIABLE MACRO=CALIBRATION_DATA VARIABLE=calibration_active VALUE=False
    
    RESPOND MSG="📋 ===== RELATÓRIO FINAL Z OFFSETS ====="
    RESPOND MSG="📅 Calibração concluída"
    RESPOND MSG=""
    
    # T0 (referência)
    {% set ref_z = printer.save_variables.variables.t0_reference_z|default(0.0)|float %}
    RESPOND MSG="{'🔧 T0 (Referência): Z = ' ~ (ref_z|round(4)) ~ 'mm (BLTouch)'}"
    
    # T1, T2 (T3 desabilitado no tools.cfg)
    {% for tool_num in range(1, 3) %}
        {% set z_offset = printer['gcode_macro TOOL_DATA']['offset_z' ~ tool_num]|default(0.0)|float %}
        {% if z_offset != 0.0 %}
            RESPOND MSG="{'🔧 T' ~ tool_num ~ ': Offset Z = ' ~ ('%+.4f'|format(z_offset)) ~ 'mm'}"
        {% else %}
            RESPOND MSG="{'⚠️ T' ~ tool_num ~ ': NÃO CALIBRADO (offset = 0.000mm)'}"
        {% endif %}
    {% endfor %}
    
    # Mostrar T3 como desabilitado se tiver offset residual
    {% set z_offset_t3 = vars.get('tool_1_offset_z', 0.0)|float %}
    {% if z_offset_t3 != 0.0 %}
        RESPOND MSG="{'🚫 T3: DESABILITADO (offset residual = ' ~ ('%+.4f'|format(z_offset_t3)) ~ 'mm)'}"
    {% else %}
        RESPOND MSG="🚫 T3: DESABILITADO (sem macro no tools.cfg)"
    {% endif %}
    
    RESPOND MSG=""
    RESPOND MSG="💡 PRÓXIMOS PASSOS:"
    RESPOND MSG="   1. Execute SAVE_CONFIG para salvar permanentemente"
    RESPOND MSG="   2. Teste com impressão de primeira camada"
    RESPOND MSG="   3. Ajuste fino se necessário com AJUSTE_FINO_Z"
    RESPOND MSG="   4. Use BACKUP_OFFSETS para criar backup"

[gcode_macro AJUSTE_FINO_Z]
description: Ajuste fino de Z offset para uma ferramenta específica
gcode:
    {% set tool = params.TOOL|default(1)|int %}
    {% set ajuste = params.AJUSTE|default(0.0)|float %}
    
    {% if ajuste == 0.0 %}
        RESPOND MSG="❌ Use: AJUSTE_FINO_Z TOOL=1 AJUSTE=-0.05"
        RESPOND MSG="💡 Valores positivos sobem, negativos descem"
        RESPOND MSG="💡 Ou use AJUSTE_FINO_Z_ATUAL AJUSTE=-0.05 para ferramenta ativa"
    {% else %}
        {% set current_offset = printer['gcode_macro TOOL_DATA']['offset_z' ~ tool]|default(0.0)|float %}
        {% set new_offset = current_offset + ajuste %}
        
        SET_GCODE_VARIABLE MACRO=TOOL_DATA VARIABLE=offset_z{tool} VALUE={new_offset}
        SAVE_VARIABLE VARIABLE=tool_{tool}_offset_z VALUE={new_offset}
        
        RESPOND MSG="{'🔧 T' ~ tool ~ ': ' ~ (current_offset|round(4)) ~ 'mm → ' ~ (new_offset|round(4)) ~ 'mm'}"
        RESPOND MSG="{'📊 Ajuste aplicado: ' ~ ('%+.4f'|format(ajuste)) ~ 'mm'}"
        RESPOND MSG="💾 Execute SAVE_CONFIG para salvar"
    {% endif %}

[gcode_macro AJUSTE_FINO_Z_ATUAL]
description: Ajuste fino de Z offset para a ferramenta atualmente ativa
gcode:
    {% set ajuste = params.AJUSTE|default(0.0)|float %}
    
    {% if ajuste == 0.0 %}
        RESPOND MSG="❌ Use: AJUSTE_FINO_Z_ATUAL AJUSTE=-0.05"
        RESPOND MSG="💡 Valores positivos sobem, negativos descem"
    {% else %}
        # Detectar ferramenta ativa atual
        {% set active_tool = printer['gcode_macro TOOL_STATE'].tool|default(1)|int %}
        
        # Converter número da ferramenta ativa para índice de offset (T0=1, T1=2, T2=3)
        {% if active_tool == 1 %}
            {% set tool_offset_index = 0 %}  # T0 não tem offset (referência)
        {% elif active_tool == 2 %}
            {% set tool_offset_index = 1 %}  # T1
        {% elif active_tool == 3 %}
            {% set tool_offset_index = 2 %}  # T2
        {% else %}
            {% set tool_offset_index = 1 %}  # Default para T1
        {% endif %}
        
        {% if tool_offset_index == 0 %}
            RESPOND MSG="⚠️ T0 é a referência (BLTouch fixo) - não pode ser ajustado"
            RESPOND MSG="💡 Use T1 ou T2 para ajustes de offset"
        {% else %}
             {% set current_offset = printer['gcode_macro TOOL_DATA']['offset_z' ~ tool_offset_index]|default(0.0)|float %}
             {% set new_offset = current_offset + ajuste %}
             
             # Aplicar o offset na memória
             SET_GCODE_VARIABLE MACRO=TOOL_DATA VARIABLE=offset_z{tool_offset_index} VALUE={new_offset}
             SAVE_VARIABLE VARIABLE=tool_{tool_offset_index}_offset_z VALUE={new_offset}
             
             # Mover fisicamente o Z para aplicar o novo offset imediatamente
             {% set current_z = printer.toolhead.position.z %}
             G91  # Modo relativo
             G1 Z{ajuste} F300  # Move Z pelo valor do ajuste
             G90  # Volta ao modo absoluto
             
             RESPOND MSG="{'🔧 T' ~ tool_offset_index ~ ' (ativa): ' ~ (current_offset|round(4)) ~ 'mm → ' ~ (new_offset|round(4)) ~ 'mm'}"
             RESPOND MSG="{'📊 Ajuste aplicado: ' ~ ('%+.4f'|format(ajuste)) ~ 'mm'}"
             RESPOND MSG="{'⬆️ Z movido fisicamente: ' ~ ('%+.4f'|format(ajuste)) ~ 'mm'}"
             RESPOND MSG="💾 Execute SAVE_CONFIG para salvar"
         {% endif %}
    {% endif %}

[gcode_macro LIMPAR_OFFSET_T3]
description: Remove offset residual do T3 desabilitado
gcode:
    RESPOND MSG="🧹 Limpando offset residual do T3 desabilitado..."
    
    SET_GCODE_VARIABLE MACRO=TOOL_DATA VARIABLE=offset_x3 VALUE=0.0
    SET_GCODE_VARIABLE MACRO=TOOL_DATA VARIABLE=offset_y3 VALUE=0.0
    # offset_z3 removido - Z lido diretamente de variables.cfg
    
    SAVE_VARIABLE VARIABLE=tool_3_offset_x VALUE=0.0
    SAVE_VARIABLE VARIABLE=tool_3_offset_y VALUE=0.0
    SAVE_VARIABLE VARIABLE=tool_3_offset_z VALUE=0.0
    
    RESPOND MSG="✅ Offsets do T3 zerados (T3 está desabilitado)"
    RESPOND MSG="💾 Execute SAVE_CONFIG para salvar permanentemente"

[gcode_macro STATUS_CALIBRACAO]
description: Mostra o status atual da calibração e próximos passos
gcode:
    RESPOND MSG="📊 ===== STATUS DA CALIBRAÇÃO ====="
    
    # Verificar qual ferramenta está sendo calibrada
    {% set current_tool = printer.save_variables.variables.current_calibrating_tool|default(0)|int %}
    {% set calibration_active = printer['gcode_macro CALIBRATION_DATA'].calibration_active|default(false) %}
    
    {% if calibration_active %}
        RESPOND MSG="{'🔧 Calibrando atualmente: T' ~ current_tool}"
        RESPOND MSG="📋 PRÓXIMOS PASSOS:"
        RESPOND MSG="{'   1. Ajuste T' ~ current_tool ~ ' com AJUSTAR_Z_BAIXO/CIMA'}"
        RESPOND MSG="{'   2. Execute CONFIRMAR_Z_T' ~ current_tool ~ ' quando perfeito'}"
        {% if current_tool < 2 %}
            RESPOND MSG="   3. Execute CALIBRAR_PROXIMA_FERRAMENTA para T{current_tool + 1}"
        {% else %}
            RESPOND MSG="   3. Execute RELATORIO_Z_OFFSETS_FINAL quando terminar"
        {% endif %}
    {% else %}
        RESPOND MSG="⚠️ Nenhuma calibração ativa"
        RESPOND MSG="💡 Execute CALIB_OFFS para iniciar calibração"
    {% endif %}
    
    RESPOND MSG=""
    # Mostrar offsets atuais
    {% set ref_z = printer.save_variables.variables.t0_reference_z|default(0.0)|float %}
    RESPOND MSG="{'🔧 T0 (Referência): Z = ' ~ (ref_z|round(4)) ~ 'mm'}"
    
    {% for tool_num in range(1, 3) %}
        {% set z_offset = printer['gcode_macro TOOL_DATA']['offset_z' ~ tool_num]|default(0.0)|float %}
        {% if z_offset != 0.0 %}
            RESPOND MSG="{'✅ T' ~ tool_num ~ ': Offset Z = ' ~ ('%+.4f'|format(z_offset)) ~ 'mm'}"
        {% else %}
            RESPOND MSG="{'⚠️ T' ~ tool_num ~ ': NÃO CALIBRADO (offset = 0.000mm)'}"
        {% endif %}
    {% endfor %}
    
    {% set vars = printer.save_variables.variables %}
    {% set z_offset_t3 = vars.get('tool_1_offset_z', 0.0)|float %}
    {% if z_offset_t3 != 0.0 %}
        RESPOND MSG="{'🚫 T3: DESABILITADO (offset residual = ' ~ ('%+.4f'|format(z_offset_t3)) ~ 'mm) - Use LIMPAR_OFFSET_T3'}"
    {% else %}
        RESPOND MSG="🚫 T3: DESABILITADO (sem macro no tools.cfg)"
    {% endif %}