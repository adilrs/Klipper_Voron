#####################################################################
#          SISTEMA DE CALIBRAÇÃO MULTI-FERRAMENTA INDEPENDENTE     #
#                    SEM INTERCEPTAÇÕES - VERSÃO 2.0               #
#####################################################################
# Este sistema NÃO intercepta comandos existentes do Klipper/KlipperScreen
# Em vez disso, fornece comandos dedicados para calibração de cada ferramenta
#
# COMANDOS PRINCIPAIS:
# - CALIBRAR_T0: Calibra ferramenta T0 (referência)
# - CALIBRAR_T1: Calibra ferramenta T1 relativa ao T0
# - CALIBRAR_T2: Calibra ferramenta T2 relativa ao T0
# - CALIBRAR_T3: Calibra ferramenta T3 relativa ao T0
#
# VANTAGENS:
# - Sem conflitos com sistema padrão
# - Controle total do processo
# - Mais simples e confiável
# - Fácil manutenção
#####################################################################

[gcode_macro CALIBRATION_STATE]
description: Estado do sistema de calibração independente
variable_calibrating_tool: 0          # Ferramenta sendo calibrada (0=nenhuma, 1=T0, 2=T1, 3=T2, 4=T3)
variable_calibration_active: False    # Se calibração está ativa
variable_reference_z_offset: 0.0      # Offset de referência do T0
gcode:
    # Macro de dados - não executa código

#####################################################################
#                    CALIBRAÇÃO T0 (REFERÊNCIA)                    #
#####################################################################

[gcode_macro CALIBRAR_T0]
description: Calibra ferramenta T0 (estabelece referência para outras ferramentas)
gcode:
    RESPOND MSG="🔧 ===== CALIBRAÇÃO T0 (REFERÊNCIA) ====="
    RESPOND MSG="📌 T0 será salvo em printer.cfg [bltouch] z_offset"
    RESPOND MSG="📋 Este valor serve como referência para T1 e T2"
    
    # Verificações de segurança
    {% if not 'xyz' in printer.toolhead.homed_axes %}
        RESPOND MSG="⚠️ Fazendo homing completo..."
        G28
    {% endif %}
    
    # Ativar T0
    RESPOND MSG="🔧 Ativando ferramenta T0..."
    T0
    G4 P2000  # Aguardar estabilização
    
    # Salvar estado
    SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibrating_tool VALUE=1
    SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibration_active VALUE=True
    
    # Posicionar no centro da mesa
    RESPOND MSG="🎯 Posicionando no centro da mesa..."
    G90
    G1 X{printer.toolhead.axis_maximum.x / 2} Y{printer.toolhead.axis_maximum.y / 2} Z10 F6000
    
    RESPOND MSG="📋 ===== INSTRUÇÕES T0 ====="
    RESPOND MSG="1. Use PROBE_CALIBRATE para iniciar calibração padrão"
    RESPOND MSG="2. Ajuste a altura com os botões do KlipperScreen"
    RESPOND MSG="3. Quando perfeito, execute: FINALIZAR_CALIBRACAO_T0"
    RESPOND MSG="4. NÃO use ACCEPT - use nosso comando personalizado"
    RESPOND MSG="💡 Comando: FINALIZAR_CALIBRACAO_T0"

[gcode_macro FINALIZAR_CALIBRACAO_T0]
description: Finaliza calibração do T0 e salva no printer.cfg
gcode:
    RESPOND MSG="💾 Finalizando calibração T0..."
    
    # Verificar se calibração está ativa
    {% set calibrating = printer['gcode_macro CALIBRATION_STATE'].calibrating_tool|int %}
    {% if calibrating != 1 %}
        RESPOND MSG="❌ ERRO: Calibração T0 não está ativa!"
        RESPOND MSG="💡 Execute CALIBRAR_T0 primeiro"
        {% else %}
        
        # Aplicar e salvar calibração
        RESPOND MSG="📝 Aplicando calibração T0..."
        Z_OFFSET_APPLY_PROBE
        
        RESPOND MSG="💾 Salvando em printer.cfg..."
        SAVE_CONFIG
        
        # Reset do estado
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibrating_tool VALUE=0
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibration_active VALUE=False
        
        RESPOND MSG="✅ T0 calibrado e salvo com sucesso!"
        RESPOND MSG="📁 Localização: printer.cfg [bltouch] z_offset"
        RESPOND MSG="🎯 Agora você pode calibrar T1 e T2 usando:"
        RESPOND MSG="   • CALIBRAR_T1"
        RESPOND MSG="   • CALIBRAR_T2"
    {% endif %}

#####################################################################
#                    CALIBRAÇÃO T1 (RELATIVA)                      #
#####################################################################

[gcode_macro CALIBRAR_T1]
description: Calibra ferramenta T1 relativa ao T0
gcode:
    RESPOND MSG="🔧 ===== CALIBRAÇÃO T1 (RELATIVA) ====="
    RESPOND MSG="📌 T1 será salvo em variables.cfg como tool_0_offset_z"
    
    # Verificar se T0 foi calibrado
    {% set t0_offset = printer.configfile.settings.bltouch.z_offset|float %}
    {% if t0_offset == 0.0 %}
        RESPOND MSG="❌ ERRO: T0 não foi calibrado ainda!"
        RESPOND MSG="💡 Execute CALIBRAR_T0 primeiro para estabelecer referência"
        RESPOND MSG="🚫 Calibração T1 cancelada"
        {% else %}
        
        # Verificações de segurança
        {% if not 'xyz' in printer.toolhead.homed_axes %}
            RESPOND MSG="⚠️ Fazendo homing completo..."
            G28
        {% endif %}
        
        # Ativar T1
        RESPOND MSG="🔧 Ativando ferramenta T1..."
        T1
        G4 P2000  # Aguardar estabilização
        
        # Salvar estado e referência
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibrating_tool VALUE=2
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibration_active VALUE=True
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=reference_z_offset VALUE={t0_offset}
        
        # Posicionar no centro da mesa
        RESPOND MSG="🎯 Posicionando no centro da mesa..."
        G90
        G1 X{printer.toolhead.axis_maximum.x / 2} Y{printer.toolhead.axis_maximum.y / 2} Z10 F6000
        
        # Mostrar informações
        {% set current_t1_offset = printer.save_variables.variables.get('tool_0_offset_z', 0.0)|float %}
        RESPOND MSG="📊 Referência T0: {t0_offset|round(4)}mm"
        RESPOND MSG="📊 Offset atual T1: {current_t1_offset|round(4)}mm"
        
        RESPOND MSG="📋 ===== INSTRUÇÕES T1 ====="
        RESPOND MSG="1. Use PROBE_CALIBRATE para iniciar calibração"
        RESPOND MSG="2. Ajuste a altura com os botões do KlipperScreen"
        RESPOND MSG="3. Quando perfeito, execute: FINALIZAR_CALIBRACAO_T1"
        RESPOND MSG="4. NÃO use ACCEPT - use nosso comando personalizado"
        RESPOND MSG="💡 Comando: FINALIZAR_CALIBRACAO_T1"
    {% endif %}

[gcode_macro FINALIZAR_CALIBRACAO_T1]
description: Finaliza calibração do T1 e salva no variables.cfg
gcode:
    RESPOND MSG="💾 Finalizando calibração T1..."
    
    # Verificar se calibração está ativa
    {% set calibrating = printer['gcode_macro CALIBRATION_STATE'].calibrating_tool|int %}
    {% if calibrating != 2 %}
        RESPOND MSG="❌ ERRO: Calibração T1 não está ativa!"
        RESPOND MSG="💡 Execute CALIBRAR_T1 primeiro"
        {% else %}
        
        # Obter valores
        {% set ref_z = printer['gcode_macro CALIBRATION_STATE'].reference_z_offset|float %}
        {% set current_z = printer.gcode_move.gcode_position.z|float %}
        {% set relative_offset = current_z - ref_z %}
        
        RESPOND MSG="📊 Calculando offset relativo T1..."
        RESPOND MSG="   Referência T0: {ref_z|round(4)}mm"
        RESPOND MSG="   Posição atual: {current_z|round(4)}mm"
        RESPOND MSG="   Offset relativo: {relative_offset|round(4)}mm"
        
        # Salvar no variables.cfg
        RESPOND MSG="💾 Salvando T1 em variables.cfg..."
        SAVE_VARIABLE VARIABLE=tool_0_offset_z VALUE={relative_offset}
        
        # Reset do estado
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibrating_tool VALUE=0
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibration_active VALUE=False
        
        RESPOND MSG="✅ T1 calibrado e salvo com sucesso!"
        RESPOND MSG="📁 Localização: variables.cfg → tool_0_offset_z = {relative_offset|round(4)}"
        RESPOND MSG="🔄 Execute RESTART para aplicar mudanças"
    {% endif %}

#####################################################################
#                    CALIBRAÇÃO T2 (RELATIVA)                      #
#####################################################################

[gcode_macro CALIBRAR_T2]
description: Calibra ferramenta T2 relativa ao T0
gcode:
    RESPOND MSG="🔧 ===== CALIBRAÇÃO T2 (RELATIVA) ====="
    RESPOND MSG="📌 T2 será salvo em variables.cfg como tool_1_offset_z"
    
    # Verificar se T0 foi calibrado
    {% set t0_offset = printer.configfile.settings.bltouch.z_offset|float %}
    {% if t0_offset == 0.0 %}
        RESPOND MSG="❌ ERRO: T0 não foi calibrado ainda!"
        RESPOND MSG="💡 Execute CALIBRAR_T0 primeiro para estabelecer referência"
        RESPOND MSG="🚫 Calibração T2 cancelada"
        {% else %}
        
        # Verificações de segurança
        {% if not 'xyz' in printer.toolhead.homed_axes %}
            RESPOND MSG="⚠️ Fazendo homing completo..."
            G28
        {% endif %}
        
        # Ativar T2
        RESPOND MSG="🔧 Ativando ferramenta T2..."
        T2
        G4 P2000  # Aguardar estabilização
        
        # Salvar estado e referência
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibrating_tool VALUE=3
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibration_active VALUE=True
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=reference_z_offset VALUE={t0_offset}
        
        # Posicionar no centro da mesa
        RESPOND MSG="🎯 Posicionando no centro da mesa..."
        G90
        G1 X{printer.toolhead.axis_maximum.x / 2} Y{printer.toolhead.axis_maximum.y / 2} Z10 F6000
        
        # Mostrar informações
        {% set current_t2_offset = printer.save_variables.variables.get('tool_1_offset_z', 0.0)|float %}
        RESPOND MSG="📊 Referência T0: {t0_offset|round(4)}mm"
        RESPOND MSG="📊 Offset atual T2: {current_t2_offset|round(4)}mm"
        
        RESPOND MSG="📋 ===== INSTRUÇÕES T2 ====="
        RESPOND MSG="1. Use PROBE_CALIBRATE para iniciar calibração"
        RESPOND MSG="2. Ajuste a altura com os botões do KlipperScreen"
        RESPOND MSG="3. Quando perfeito, execute: FINALIZAR_CALIBRACAO_T2"
        RESPOND MSG="4. NÃO use ACCEPT - use nosso comando personalizado"
        RESPOND MSG="💡 Comando: FINALIZAR_CALIBRACAO_T2"
    {% endif %}

[gcode_macro FINALIZAR_CALIBRACAO_T2]
description: Finaliza calibração do T2 e salva no variables.cfg
gcode:
    RESPOND MSG="💾 Finalizando calibração T2..."
    
    # Verificar se calibração está ativa
    {% set calibrating = printer['gcode_macro CALIBRATION_STATE'].calibrating_tool|int %}
    {% if calibrating != 3 %}
        RESPOND MSG="❌ ERRO: Calibração T2 não está ativa!"
        RESPOND MSG="💡 Execute CALIBRAR_T2 primeiro"
        {% else %}
        
        # Obter valores
        {% set ref_z = printer['gcode_macro CALIBRATION_STATE'].reference_z_offset|float %}
        {% set current_z = printer.gcode_move.gcode_position.z|float %}
        {% set relative_offset = current_z - ref_z %}
        
        RESPOND MSG="📊 Calculando offset relativo T2..."
        RESPOND MSG="   Referência T0: {ref_z|round(4)}mm"
        RESPOND MSG="   Posição atual: {current_z|round(4)}mm"
        RESPOND MSG="   Offset relativo: {relative_offset|round(4)}mm"
        
        # Salvar no variables.cfg
        RESPOND MSG="💾 Salvando T2 em variables.cfg..."
        SAVE_VARIABLE VARIABLE=tool_1_offset_z VALUE={relative_offset}
        
        # Reset do estado
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibrating_tool VALUE=0
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibration_active VALUE=False
        
        RESPOND MSG="✅ T2 calibrado e salvo com sucesso!"
        RESPOND MSG="📁 Localização: variables.cfg → tool_1_offset_z = {relative_offset|round(4)}"
        RESPOND MSG="🔄 Execute RESTART para aplicar mudanças"
    {% endif %}

#####################################################################
#                    CALIBRAÇÃO T3 (RELATIVA)                      #
#####################################################################

[gcode_macro CALIBRAR_T3]
description: Calibra ferramenta T3 relativa ao T0
gcode:
    RESPOND MSG="🔧 ===== CALIBRAÇÃO T3 (RELATIVA) ====="
    RESPOND MSG="📌 T3 será salvo em variables.cfg como tool_2_offset_z"
    
    # Verificar se T0 foi calibrado
    {% set t0_offset = printer.configfile.settings.bltouch.z_offset|float %}
    {% if t0_offset == 0.0 %}
        RESPOND MSG="❌ ERRO: T0 não foi calibrado ainda!"
        RESPOND MSG="💡 Execute CALIBRAR_T0 primeiro para estabelecer referência"
        RESPOND MSG="🚫 Calibração T3 cancelada"
        {% else %}
        
        # Verificações de segurança
        {% if not 'xyz' in printer.toolhead.homed_axes %}
            RESPOND MSG="⚠️ Fazendo homing completo..."
            G28
        {% endif %}
        
        # Ativar T3
        RESPOND MSG="🔧 Ativando ferramenta T3..."
        T3
        G4 P2000  # Aguardar estabilização
        
        # Salvar estado e referência
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibrating_tool VALUE=4
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibration_active VALUE=True
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=reference_z_offset VALUE={t0_offset}
        
        # Posicionar no centro da mesa
        RESPOND MSG="🎯 Posicionando no centro da mesa..."
        G90
        G1 X{printer.toolhead.axis_maximum.x / 2} Y{printer.toolhead.axis_maximum.y / 2} Z10 F6000
        
        # Mostrar informações
        {% set current_t3_offset = printer.save_variables.variables.get('tool_2_offset_z', 0.0)|float %}
        RESPOND MSG="📊 Referência T0: {t0_offset|round(4)}mm"
        RESPOND MSG="📊 Offset atual T3: {current_t3_offset|round(4)}mm"
        
        RESPOND MSG="📋 ===== INSTRUÇÕES T3 ====="
        RESPOND MSG="1. Use PROBE_CALIBRATE para iniciar calibração"
        RESPOND MSG="2. Ajuste a altura com os botões do KlipperScreen"
        RESPOND MSG="3. Quando perfeito, execute: FINALIZAR_CALIBRACAO_T3"
        RESPOND MSG="4. NÃO use ACCEPT - use nosso comando personalizado"
        RESPOND MSG="💡 Comando: FINALIZAR_CALIBRACAO_T3"
    {% endif %}

[gcode_macro FINALIZAR_CALIBRACAO_T3]
description: Finaliza calibração do T3 e salva no variables.cfg
gcode:
    RESPOND MSG="💾 Finalizando calibração T3..."
    
    # Verificar se calibração está ativa
    {% set calibrating = printer['gcode_macro CALIBRATION_STATE'].calibrating_tool|int %}
    {% if calibrating != 4 %}
        RESPOND MSG="❌ ERRO: Calibração T3 não está ativa!"
        RESPOND MSG="💡 Execute CALIBRAR_T3 primeiro"
        {% else %}
        
        # Obter valores
        {% set ref_z = printer['gcode_macro CALIBRATION_STATE'].reference_z_offset|float %}
        {% set current_z = printer.gcode_move.gcode_position.z|float %}
        {% set relative_offset = current_z - ref_z %}
        
        RESPOND MSG="📊 Calculando offset relativo T3..."
        RESPOND MSG="   Referência T0: {ref_z|round(4)}mm"
        RESPOND MSG="   Posição atual: {current_z|round(4)}mm"
        RESPOND MSG="   Offset relativo: {relative_offset|round(4)}mm"
        
        # Salvar no variables.cfg
        RESPOND MSG="💾 Salvando T3 em variables.cfg..."
        SAVE_VARIABLE VARIABLE=tool_2_offset_z VALUE={relative_offset}
        
        # Reset do estado
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibrating_tool VALUE=0
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibration_active VALUE=False
        
        RESPOND MSG="✅ T3 calibrado e salvo com sucesso!"
        RESPOND MSG="📁 Localização: variables.cfg → tool_2_offset_z = {relative_offset|round(4)}"
        RESPOND MSG="🔄 Execute RESTART para aplicar mudanças"
    {% endif %}

#####################################################################
#                    UTILITÁRIOS E DIAGNÓSTICO                     #
#####################################################################

[gcode_macro STATUS_CALIB_INDEP]
description: Mostra status do sistema de calibração independente
gcode:
    RESPOND MSG="📊 ===== STATUS CALIBRAÇÃO INDEPENDENTE ====="
    
    # Status do sistema
    {% set calibrating = printer['gcode_macro CALIBRATION_STATE'].calibrating_tool|int %}
    {% set active = printer['gcode_macro CALIBRATION_STATE'].calibration_active %}
    
    {% if active %}
        {% if calibrating == 1 %}
            RESPOND MSG="🔧 Calibrando: T0 (referência)"
        {% elif calibrating == 2 %}
            RESPOND MSG="🔧 Calibrando: T1 (relativa)"
        {% elif calibrating == 3 %}
            RESPOND MSG="🔧 Calibrando: T2 (relativa)"
        {% elif calibrating == 4 %}
            RESPOND MSG="🔧 Calibrando: T3 (relativa)"
        {% endif %}
    {% else %}
        {% endif %}
        {% endif %}
        {% endif %}
        RESPOND MSG="💤 Sistema inativo"
    {% endif %}
    
    # Offsets atuais
    {% set t0_offset = printer.configfile.settings.bltouch.z_offset|float %}
    {% set t1_offset = printer.save_variables.variables.get('tool_0_offset_z', 0.0)|float %}
    {% set t2_offset = printer.save_variables.variables.get('tool_1_offset_z', 0.0)|float %}
    {% set t3_offset = printer.save_variables.variables.get('tool_2_offset_z', 0.0)|float %}
    
    RESPOND MSG="📊 OFFSETS ATUAIS:"
    RESPOND MSG="   T0 (referência): {t0_offset|round(4)}mm [printer.cfg]"
    RESPOND MSG="   T1 (relativo): {t1_offset|round(4)}mm [variables.cfg]"
    RESPOND MSG="   T2 (relativo): {t2_offset|round(4)}mm [variables.cfg]"
    RESPOND MSG="   T3 (relativo): {t3_offset|round(4)}mm [variables.cfg]"
    
    RESPOND MSG="💡 COMANDOS DISPONÍVEIS:"
    RESPOND MSG="   • CALIBRAR_T0 - Calibrar ferramenta T0"
    RESPOND MSG="   • CALIBRAR_T1 - Calibrar ferramenta T1"
    RESPOND MSG="   • CALIBRAR_T2 - Calibrar ferramenta T2"
    RESPOND MSG="   • CALIBRAR_T3 - Calibrar ferramenta T3"
    RESPOND MSG="   • STATUS_CALIBRACAO_INDEPENDENTE - Este status"

[gcode_macro CANCELAR_CALIBRACAO]
description: Cancela calibração em andamento
gcode:
    {% set active = printer['gcode_macro CALIBRATION_STATE'].calibration_active %}
    {% if active %}
        RESPOND MSG="🚫 Cancelando calibração em andamento..."
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibrating_tool VALUE=0
        SET_GCODE_VARIABLE MACRO=CALIBRATION_STATE VARIABLE=calibration_active VALUE=False
        RESPOND MSG="✅ Calibração cancelada"
    {% else %}
        RESPOND MSG="💤 Nenhuma calibração ativa para cancelar"
    {% endif %}

#####################################################################
#                    INFORMAÇÕES E AJUDA                           #
#####################################################################

[gcode_macro AJUDA_CALIB_INDEP]
description: Mostra ajuda do sistema de calibração independente
gcode:
    RESPOND MSG="📖 ===== SISTEMA CALIBRAÇÃO INDEPENDENTE ====="
    RESPOND MSG="🎯 VANTAGENS:"
    RESPOND MSG="   • Sem interceptações problemáticas"
    RESPOND MSG="   • Controle total do processo"
    RESPOND MSG="   • Comandos dedicados para cada ferramenta"
    RESPOND MSG="   • Mais simples e confiável"
    RESPOND MSG=""
    RESPOND MSG="📋 FLUXO DE CALIBRAÇÃO:"
    RESPOND MSG="   1. CALIBRAR_T0 → Estabelece referência"
    RESPOND MSG="   2. CALIBRAR_T1 → Calibra T1 relativo ao T0"
    RESPOND MSG="   3. CALIBRAR_T2 → Calibra T2 relativo ao T0"
    RESPOND MSG="   4. CALIBRAR_T3 → Calibra T3 relativo ao T0"
    RESPOND MSG=""
    RESPOND MSG="🔧 PROCESSO PARA CADA FERRAMENTA:"
    RESPOND MSG="   1. Execute CALIBRAR_TX (onde X = 0, 1, 2 ou 3)"
    RESPOND MSG="   2. Execute PROBE_CALIBRATE no terminal"
    RESPOND MSG="   3. Ajuste altura com botões do KlipperScreen"
    RESPOND MSG="   4. Execute FINALIZAR_CALIBRACAO_TX"
    RESPOND MSG=""
    RESPOND MSG="💾 ONDE SALVA:"
    RESPOND MSG="   • T0: printer.cfg [bltouch] z_offset"
    RESPOND MSG="   • T1: variables.cfg tool_0_offset_z"
    RESPOND MSG="   • T2: variables.cfg tool_1_offset_z"
    RESPOND MSG="   • T3: variables.cfg tool_2_offset_z"
    RESPOND MSG=""
    RESPOND MSG="💡 COMANDOS ÚTEIS:"
    RESPOND MSG="   • STATUS_CALIBRACAO_INDEPENDENTE"
    RESPOND MSG="   • CANCELAR_CALIBRACAO"
    RESPOND MSG="   • AJUDA_CALIBRACAO_INDEPENDENTE"